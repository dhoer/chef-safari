---
driver:
  name: vagrant
  provider: vmware_fusion
  customize:
      memory: 2048

provisioner:
  name: chef_solo
  require_chef_omnibus: 11

platforms:
  - name: macosx-10.10
    driver:
      name: localhost

suites:
  - name: safari_test
    run_list:
      - recipe[safari_test::extension]
      - recipe[safari_test::version]
    attributes:
